apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: k8s-app
data:
  REDIS_URL: "redis://redis-service:6379"
  PORT: "3000"
  NODE_ENV: "production"
  # Set to the NLB DNS name after first deploy, then redeploy backend.
  ALLOWED_ORIGIN: "<ALLOWED_ORIGIN>"
