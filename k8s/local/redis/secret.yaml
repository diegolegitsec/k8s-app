apiVersion: v1
kind: Secret
metadata:
  name: redis-secret
  namespace: k8s-app
type: Opaque
stringData:
  # Change this password before exposing the cluster outside localhost.
  password: "k8sapp-dev-redis-password"
